PROJECT

Repository:
https://github.com/canercetinkaya/cloudshield-mssp-portal

Current release:
v2.5.10-PILOT

Target release candidate:
v2.5.11-PILOT

ROLE

Act as the Principal CloudShield Platform Engineer, DevSecOps Lead,
Application Security Engineer, Release Engineer, QA Automation Engineer,
PowerShell 7 Engineer, and Python API Engineer.

PRIMARY OBJECTIVE

Inspect the actual current repository and remediate the remaining release,
encoding, tenant isolation, report authorization, UI data integrity,
test integrity, and Git synchronization issues identified in v2.5.10-PILOT.

This is an implementation task.

Do not respond with only recommendations, a plan, or sample code.

Make the required repository changes, add or update tests, run the complete
validation suite, and report the actual results.

Do not ask for confirmation between phases.

Do not remove working functionality.

Do not rewrite unrelated components.

Do not claim success unless the applicable check was executed.

==================================================
CURRENT VERIFIED BASELINE
==================================================

The repository already contains:

- root-level version.json
- release v2.5.10-PILOT
- pilot release channel
- productionReady false
- build 2026.09.10.10
- commit metadata
- Python REST API gateway
- PowerShell 7 reporting engine
- service plug-in architecture
- tenant-scoped portal roles
- PDF and HTML report generation
- report download authorization checks
- concurrency tests
- temporary configuration isolation tests
- immutable tag behavior
- allow-list-based Git staging
- 59 recorded passing QA checks

Preserve these capabilities.

Do not regress:

- version manifest loading
- tenant authorization
- CustomerCISO tenant scoping
- CustomerViewer isolation
- unknown tenant 404 handling
- cross-tenant report generation 403 handling
- cross-tenant report download 403 handling
- concurrent report generation
- temporary config isolation
- temporary config cleanup
- PDF and HTML generation
- existing MDE and MDO report generation

==================================================
PHASE 0: REPOSITORY AND BASELINE VERIFICATION
==================================================

Before changing code:

1. Inspect the current Git branch and working tree.
2. Record uncommitted changes.
3. Read version.json.
4. Read README.md.
5. Inspect:
   - Portal/api
   - Portal/index.html or the active frontend entry point
   - Engine
   - Engine/Core
   - Engine/Plugins
   - test_comprehensive_qa.py
   - Engine/Tests
   - Watch-AndSyncToGitHub.ps1
   - Update-Version.py
   - GitHub Actions workflows
   - report download API
   - report generator
   - authentication implementation
6. Run the existing QA suite before modifications.
7. Save the baseline to:

docs/reviews/v2.5.11-baseline.md

The baseline must include:

- current commit
- current version
- current branch
- changed files
- existing test count
- existing test result
- known failing checks
- detected text encoding
- current report download route
- current sync branch behavior
- current release parser behavior

If the actual repository differs from this prompt, use the repository as the
source of truth and document the difference.

==================================================
QUALITY GATE 1: VERSION CONSISTENCY
==================================================

PROBLEM

The root version manifest correctly identifies:

- version 2.5.10
- release v2.5.10-PILOT
- channel pilot
- productionReady false

However, repository documentation or diagrams may still contain LIVE labels.

REQUIRED CHANGES

1. Treat version.json as the only version source.
2. Remove hard-coded release numbers from:
   - README diagrams
   - HTML badges
   - JavaScript defaults
   - Python endpoint code
   - PowerShell output
   - report footer
   - QA assertions
   - deployment scripts
3. Load current version metadata dynamically at runtime where practical.
4. Generated documentation may be updated from the manifest during a release.
5. Do not show v2.5.10-LIVE anywhere when the manifest says v2.5.10-PILOT.
6. Avoid embedding current release values in architecture diagrams.

Use a generic diagram label such as:

Dynamic Release Badge

instead of:

Live Release Badge: v2.5.10-LIVE

7. Add an automated repository consistency test that searches tracked
   production files for conflicting release names.
8. Allow historical release names only inside an explicitly marked changelog.

ACCEPTANCE CRITERIA

- API version matches version.json.
- Health endpoint matches version.json.
- UI badge matches version.json.
- README active release matches version.json.
- Generated report metadata matches version.json.
- QA metadata matches version.json.
- No active component contains a conflicting LIVE label.
- productionReady remains false.
- channel remains pilot.

==================================================
QUALITY GATE 2: SAFE VERSION UPDATE PROTOCOL
==================================================

PROBLEM

Watch-AndSyncToGitHub.ps1 may still parse only version strings ending in LIVE
and may contain a fixed LIVE fallback release.

REQUIRED CHANGES

1. Update Update-Version.py to emit structured JSON only.

Required output schema:

{
  "version": "2.5.11",
  "release": "v2.5.11-PILOT",
  "build": "2026.09.10.11",
  "channel": "pilot",
  "summary": "description",
  "success": true
}

2. Stop parsing version updates with a regex restricted to LIVE.
3. In PowerShell, parse the response with ConvertFrom-Json.
4. Accept only these release channels:
   - DEV
   - INTERNAL
   - PILOT
   - PRODUCTION
5. Validate that the returned channel matches version.json policy.
6. Remove all fixed fallback release values such as:
   - v2.5.2-LIVE
   - v2.5.10-LIVE
7. If version update output is invalid:
   - do not create a tag
   - do not commit a release version
   - do not push
   - exit with nonzero status
   - write a sanitized error
8. Do not silently recover by inventing a release number.
9. Add tests for:
   - PILOT output
   - PRODUCTION output
   - invalid JSON
   - missing release
   - mismatched channel
   - duplicate version
   - existing immutable tag

ACCEPTANCE CRITERIA

- PILOT release output is parsed correctly.
- No LIVE-only regex remains.
- No fixed release fallback remains.
- Invalid version output stops synchronization safely.
- Existing tags are never overwritten.
- Release tag equals the version manifest release exactly.

==================================================
QUALITY GATE 3: STOP DIRECT MAIN PUSH
==================================================

PROBLEM

Automatic workspace synchronization may still push directly to main.

REQUIRED CHANGES

1. Watch-AndSyncToGitHub.ps1 must not default to main.
2. Generate a unique synchronization branch:

sync/yyyyMMdd-HHmmss-shortsha

3. Stage only approved paths.
4. Commit to the synchronization branch.
5. Push the synchronization branch.
6. Do not merge automatically.
7. Do not create a release tag from a synchronization branch.
8. A release tag may only be created after:
   - merge to main
   - successful CI
   - explicit release command
9. Separate responsibilities into logical commands or scripts:

- Watch-WorkspaceChanges
- Sync-WorkspaceBranch
- Test-ReleaseCandidate
- New-CloudShieldRelease
- Deploy-CloudShieldRelease

These may be separate functions or files according to repository conventions.

10. Preserve developer changes.
11. Do not force checkout over uncommitted work.
12. Do not use:
   - git add .
   - git push --force
   - git tag -f
   - automatic direct main push
13. Validate the staged file list before commit.
14. Reject the synchronization if a staged path matches:
   - .git
   - __pycache__
   - node_modules
   - Logs
   - Output
   - temp
   - *.local.json
   - auth.local.json
   - certificates
   - private keys
   - execution transcripts
   - access-token output
15. Print the synchronization branch and file list.

ACCEPTANCE CRITERIA

- File watcher cannot push directly to main.
- File watcher cannot create release tags.
- Synchronization produces a separate branch.
- Unsafe files are rejected.
- Release creation is a separate explicit operation.
- Existing main history and tags are immutable.

==================================================
QUALITY GATE 4: UTF-8 END-TO-END CORRECTION
==================================================

PROBLEM

Current QA output and generated HTML/PDF reports contain mojibake such as:

- ModÃ¼l
- MÃ¼ÅŸteri
- YÃ¶netilen
- GÃ¼venliÄŸi
- Ã‡Ã¶zÃ¼ldÃ¼

This is a pilot-blocking report quality issue.

REQUIRED CHANGES

1. Audit text encoding across:
   - Python files
   - PowerShell files
   - JSON
   - HTML
   - Markdown
   - CSS
   - generated HTML
   - generated PDF input
   - QA output
2. Convert text source files to UTF-8 consistently.
3. Do not repeatedly decode or encode correctly formed Unicode text.
4. Python:
   - use encoding="utf-8" for text files
   - use ensure_ascii=False for customer-facing JSON
   - set subprocess text encoding explicitly
   - decode HTTP and process output correctly
5. PowerShell:
   - require PowerShell 7
   - set console output encoding to UTF-8
   - write JSON, HTML and logs using UTF-8
   - avoid Windows PowerShell-specific default encoding behavior
6. HTML:
   - retain meta charset UTF-8
   - confirm generated content itself is valid Unicode before rendering
7. PDF:
   - validate source HTML before Chromium rendering
   - confirm Turkish glyphs are rendered correctly
8. Do not implement broad string replacement such as:
   - replace Ã¶ with ö
   - replace Ã¼ with ü

Fix the actual encoding boundary instead.

9. Add an encoding test corpus containing:

KoçSistem
Müşteri
Yönetilen Güvenlik
Çözüldü
Şüpheli E-posta
İç Tehdit
İletişim Uyumu
Sınıflandırma
Ağustos
İstanbul
Çağrı
Ölçüm

10. Test the corpus through:

- Python JSON write/read
- REST API response
- PowerShell subprocess
- PowerShell JSON output
- HTML report
- PDF text extraction where supported
- QA result JSON

11. Fail QA if mojibake signatures are found:

- Ã
- Ä
- Å
- Â
- �

Do not flag legitimately encoded binary data.

12. Regenerate sample reports after the fix.

ACCEPTANCE CRITERIA

- QA results contain correct Turkish text.
- Generated HTML contains correct Turkish text.
- Generated PDF visibly and extractably contains correct Turkish text.
- API responses use UTF-8.
- No customer-facing mojibake remains.
- Encoding guard test passes.

==================================================
QUALITY GATE 5: REPORT-ID BASED AUTHORIZED DOWNLOAD
==================================================

PROBLEM

The report download API currently exposes or accepts a file path pattern such as:

/api/reports/download?file=customer/period/report.pdf

Even with an authorization check, accepting a client-supplied storage path is
unnecessarily risky.

REQUIRED CHANGES

1. Replace public path-based report download with:

GET /api/reports/{reportId}/download

2. Create or use a server-side report registry.

Required logical fields:

- reportId
- tenantId
- customerId
- period
- serviceCodes
- artifactType
- storageKey
- createdAtUtc
- createdBy
- reportStatus
- integrityHash
- sizeBytes
- expiresAtUtc
- version
- commit

3. The client must never provide:
   - physical file path
   - relative path
   - customer folder
   - storage key
4. Resolve the storage location server-side.
5. Before serving the file:
   - authenticate user
   - authorize tenant
   - authorize report access
   - validate report state
   - validate storage key is inside the configured report root
   - optionally validate integrity hash
6. Return:
   - 404 for unknown report
   - 403 for unauthorized report
   - 410 for expired report where applicable
7. Prevent:
   - ../ traversal
   - encoded traversal
   - absolute paths
   - alternate separators
   - double decoding
   - null byte patterns
8. Preserve backward compatibility only through a temporary server-side redirect
   if necessary.
9. Do not expose internal path details in the response.
10. Update frontend links and QA tests to use reportId.
11. Add tests:
   - authorized report download
   - cross-tenant report download
   - unknown report ID
   - expired report
   - traversal payload
   - encoded traversal
   - tampered report ID
   - invalid artifact type

ACCEPTANCE CRITERIA

- Successful generation returns reportId.
- Frontend uses reportId.
- Public path query parameter is no longer needed.
- Client cannot choose storage location.
- Cross-tenant download remains 403.
- Traversal tests pass.
- Existing report generation remains functional.

==================================================
QUALITY GATE 6: REMOVE STATIC LIVE METRICS
==================================================

PROBLEM

The frontend currently includes static values that can appear as real
customer telemetry before API loading or after an API failure.

Examples include:

- endpoint count
- MDE health
- ghost devices
- critical alerts
- MTTR
- Secure Score
- benchmarks
- daily synchronization time

REQUIRED CHANGES

1. Remove static numeric security metrics from production HTML.
2. Initial values must be:
   - Loading
   - N/A
   - Not collected
3. Use skeleton states where appropriate.
4. After API loading:
   - show real value
   - show collectedAtUtc
   - show data origin
   - show collection status
5. On API failure:
   - do not preserve old HTML defaults
   - show DataUnavailable
   - show the last successful synchronization only if it comes from server state
6. Display metric status:
   - Live
   - Cached
   - Stale
   - NoData
   - CollectionFailed
   - NotLicensed
   - PermissionMissing
7. Do not describe a dashboard as live if data is cached or stale.
8. Replace hard-coded quarterly period labels with dynamic period data.
9. Replace hard-coded last-sync values with API output.
10. Add frontend tests verifying that no security metric is populated from
    static markup.

ACCEPTANCE CRITERIA

- View source contains no production security numbers.
- Loading state is explicit.
- API failure does not display static metrics.
- Data timestamp and collection status are visible.
- Tenant-specific values remain tenant-scoped.

==================================================
QUALITY GATE 7: REMOVE PERSONAL DEFAULT USER DATA
==================================================

PROBLEM

The frontend may contain a hard-coded personal display name, initials, or
platform role before authentication.

REQUIRED CHANGES

1. Remove hard-coded:
   - personal display name
   - initials
   - email
   - platform role
   - tenant assignments
2. Keep the user profile area empty or show a neutral loading state before
   authentication.
3. Populate the profile only from the authenticated server response.
4. Use server-approved fields:
   - displayName
   - initials
   - role display name
   - assigned tenant count
5. Do not trust role or tenant fields from browser storage.
6. Clear profile data on logout.
7. Clear tenant selector and cached tenant data on logout.
8. Add tests verifying:
   - no personal name in initial HTML
   - authenticated name appears after login
   - role is server-derived
   - logout clears profile
   - one user cannot inherit another user’s profile in the same browser session

ACCEPTANCE CRITERIA

- No personal identity exists in initial production HTML.
- User identity is populated only after authentication.
- Logout clears identity and tenant state.

==================================================
QUALITY GATE 8: QA RESULT INTEGRITY
==================================================

PROBLEM

Engine test durations appear to be calculated by dividing total process time
equally among parsed PASS lines.

The PowerShell test result is currently inferred from console output.

REQUIRED CHANGES

1. Make the PowerShell test suite emit a structured JSON result.

Required schema:

{
  "runId": "uuid",
  "version": "2.5.11",
  "release": "v2.5.11-PILOT",
  "commit": "sha",
  "startedAtUtc": "timestamp",
  "completedAtUtc": "timestamp",
  "exitCode": 0,
  "tests": [
    {
      "id": "stable-test-id",
      "name": "test name",
      "category": "category",
      "status": "PASS",
      "durationMs": 25.4,
      "details": "sanitized details"
    }
  ]
}

2. Measure each test duration individually.
3. Python must read the JSON result rather than parsing PASS text.
4. Fail the full QA run if:
   - PowerShell exit code is nonzero
   - result JSON is missing
   - result JSON is invalid
   - no tests are returned
   - expected test IDs are missing
   - duplicate test IDs exist
   - version differs from version.json
   - release differs from version.json
   - commit differs from the tested commit
5. Preserve console-friendly PASS/FAIL display, but do not treat console text as
   the authoritative test protocol.
6. Add metadata:
   - operating system
   - Python version
   - PowerShell version
   - test runner
   - commit
   - version manifest hash
7. Do not commit local QA result files unless the repository explicitly treats
   them as release evidence.
8. Prefer CI-generated QA artifacts associated with the commit.
9. If qa_test_results.json remains tracked, update it only in the release
   workflow after all tests pass.

ACCEPTANCE CRITERIA

- Test durations are real per-test durations.
- PowerShell result is structured JSON.
- QA fails on missing or stale engine results.
- QA result is tied to commit and version manifest.
- Mojibake is absent from QA output.

==================================================
QUALITY GATE 9: PWSH 7 ENFORCEMENT
==================================================

PROBLEM

The platform requires PowerShell 7.4+, but tests or automation may still invoke
powershell.exe or use ExecutionPolicy Bypass.

REQUIRED CHANGES

1. Resolve pwsh explicitly.
2. Validate version is 7.4 or higher.
3. Fail clearly if pwsh is unavailable.
4. Do not silently fall back to Windows PowerShell.
5. Update:
   - Python test runner
   - local startup
   - scheduled execution
   - report worker
   - CI
6. Remove unnecessary ExecutionPolicy Bypass.
7. If CI requires policy handling, document the exact reason and constrain it
   to the CI environment only.
8. Add a PowerShell runtime test.

ACCEPTANCE CRITERIA

- All engine tests run with pwsh.
- Runtime version is included in QA metadata.
- Windows PowerShell fallback is absent.
- Production execution does not use Bypass.

==================================================
QUALITY GATE 10: REPORT DATA PROVENANCE
==================================================

PROBLEM

Generated reports contain large KPI values and operational benefit statements,
but every visible KPI must be traceable to live collection, calculation, or an
approved manual source.

REQUIRED CHANGES

1. Every KPI result must contain:

- kpiId
- serviceCode
- value
- unit
- sourceQueryIds
- dataOrigin
- collectionStatus
- collectedAtUtc
- periodStartUtc
- periodEndUtc
- calculationVersion
- evidenceId
- synthetic
- validationStatus

2. Valid dataOrigin values:

- MicrosoftApi
- MicrosoftPowerShell
- AdvancedHunting
- ManagementActivityApi
- ApprovedManualActivity
- Calculated
- NotAvailable

3. Valid collectionStatus values:

- Success
- PartialSuccess
- NoData
- PermissionMissing
- NotLicensed
- SourceUnsupported
- CollectionFailed

4. A KPI with synthetic=true must never appear in:
   - pilot customer report
   - production customer report
   - executive summary
   - email
5. A KPI without evidence or source query must not be described as live.
6. Do not convert missing data to zero.
7. Do not show a tenant-wide result when collection is partial.
8. Attach data completeness status to each service section.
9. Add a technical appendix containing the sanitized source query IDs and
   collection statuses.
10. Avoid exposing raw API records.

ACCEPTANCE CRITERIA

- Visible KPI values have provenance.
- Synthetic data is excluded from pilot report.
- Missing data is displayed accurately.
- Report indicates collection completeness.
- Live and calculated metrics are distinguishable.

==================================================
QUALITY GATE 11: REMOVE ABSOLUTE COMPLIANCE CLAIMS
==================================================

PROBLEM

Generated reports may state that the report or platform is 100 percent compliant
with KVKK, GDPR, ISO/IEC 27001, or BDDK requirements.

The implementation alone cannot establish total legal or regulatory compliance.

REQUIRED CHANGES

1. Remove statements such as:
   - yüzde 100 uyumlu
   - fully compliant
   - guaranteed compliance
   - zero regulatory risk
2. Replace them with a neutral statement:

Turkish:

"Bu rapor, veri minimizasyonu, erişim kontrolü, maskeleme ve denetim izi
ilkeleri dikkate alınarak teknik bir güvenlik çıktısı olarak hazırlanmıştır.
Mevzuat ve standart uygunluğuna ilişkin nihai değerlendirme, kurumun hukuk,
uyum, iç denetim ve veri sorumlusu ekiplerinin kapsam ve kontrol doğrulamasına
tabidir."

English:

"This report is produced as a technical security output with data minimization,
access control, masking, and auditability principles in mind. Final assessment
of regulatory or standards compliance remains subject to validation by the
organization's legal, compliance, internal audit, and data-controller functions."

3. Do not assert that SHA-256 alone provides nonrepudiation.
4. Distinguish:
   - integrity hash
   - trusted timestamp
   - digital signature
   - immutable storage
   - nonrepudiation
5. If the report only generates a local SHA-256 hash and writes a JSONL log,
   describe it as an integrity verification record, not indisputable
   nonrepudiation.
6. Add report wording tests to reject prohibited absolute claims.

ACCEPTANCE CRITERIA

- No absolute compliance claim remains.
- Hash purpose is described accurately.
- Legal and compliance validation responsibility is stated.
- Report disclaimer renders correctly in Turkish and English.

==================================================
QUALITY GATE 12: FRONTEND SECURITY AND SUPPLY CHAIN
==================================================

Review the current frontend dependencies.

If the production frontend loads Tailwind, Font Awesome, or fonts from public
CDNs:

1. Document the supply-chain and availability risk.
2. Prefer self-hosted pinned production assets.
3. If external assets remain:
   - pin exact versions
   - add Subresource Integrity where supported
   - configure Content Security Policy
   - avoid runtime-generated Tailwind in production
4. Define CSP covering:
   - default-src
   - script-src
   - style-src
   - font-src
   - img-src
   - connect-src
   - frame-ancestors
   - base-uri
   - form-action
5. Do not break the current portal design.
6. Add security header tests.

ACCEPTANCE CRITERIA

- Production dependency strategy is documented.
- CSP is present and tested.
- Unsafe inline behavior is reduced or explicitly documented.
- External dependencies are pinned or self-hosted.

==================================================
QUALITY GATE 13: RELEASE AUTOMATION
==================================================

Create an explicit release command.

Suggested interface:

pwsh ./New-CloudShieldRelease.ps1 `
  -Channel PILOT `
  -Summary "fix: pilot hardening and encoding remediation"

The release command must:

1. Require a clean working tree.
2. Require main branch.
3. Pull or fetch current remote state.
4. Verify local main matches expected remote state.
5. Run:
   - Python syntax checks
   - PowerShell syntax checks
   - ScriptAnalyzer
   - complete QA
   - version consistency
   - secret scan where locally available
   - encoding checks
   - report smoke test
6. Stop if any check fails.
7. Increment version through version.json.
8. Update generated documentation.
9. Commit release metadata.
10. Create an immutable annotated tag.
11. Never force a tag.
12. Push only after all gates pass.
13. Do not automatically mark productionReady true.
14. Production readiness requires a distinct approved release path.

ACCEPTANCE CRITERIA

- File synchronization and release are separate.
- Failed quality gates prevent release.
- Tags are immutable.
- Pilot remains non-production.
- Release metadata records commit and test run ID.

==================================================
REQUIRED NEW TESTS
==================================================

Add tests for all of the following:

VERSION

- version manifest parses
- API version matches
- health version matches
- UI release matches
- README release matches
- no conflicting active release string
- productionReady remains false for PILOT

SYNC AND RELEASE

- sync branch is not main
- sync cannot tag
- release requires main
- invalid version JSON blocks release
- PILOT parser works
- immutable tag is preserved
- unsafe path is rejected
- secret-like file is rejected

ENCODING

- Turkish test corpus JSON round trip
- API UTF-8
- PowerShell UTF-8
- HTML UTF-8
- PDF text or visual validation
- QA result UTF-8
- mojibake signature rejection

REPORT DOWNLOAD

- report ID success
- unknown report
- unauthorized report
- cross-tenant report
- expired report
- traversal
- encoded traversal
- absolute path
- invalid artifact type

FRONTEND DATA

- no static endpoint count
- no static alert count
- no static MTTR
- no static Secure Score
- no personal default identity
- loading state shown
- failure state shown
- cached/stale state shown correctly

QA PROTOCOL

- structured PowerShell JSON
- actual per-test durations
- nonzero PowerShell exit code fails
- stale commit fails
- manifest mismatch fails
- duplicate test ID fails
- missing test fails

REPORT PROVENANCE

- KPI source query present
- synthetic KPI excluded
- missing KPI not converted to zero
- PartialSuccess disclosed
- ApprovedManualActivity requires approval
- absolute compliance wording rejected

==================================================
REQUIRED VALIDATION COMMANDS
==================================================

Use the repository's actual commands after inspection.

At minimum execute the equivalent of:

1. Python compilation:

python -m py_compile Portal/api/server.py Portal/api/report_generator.py

2. PowerShell version:

pwsh -NoProfile -Command "$PSVersionTable.PSVersion"

3. PowerShell tests:

pwsh -NoProfile -File Engine/Tests/Test-Platform.ps1

4. Comprehensive QA:

python test_comprehensive_qa.py

5. Version consistency test.

6. Encoding validation.

7. Report generation smoke test.

8. Authorized report download test.

9. Cross-tenant report download rejection.

10. Synchronization dry run.

11. Release dry run.

If a tool is unavailable, record the check as Blocked.
Do not mark it Passed.

==================================================
OUTPUT ARTIFACTS
==================================================

Create or update:

- docs/reviews/v2.5.11-baseline.md
- docs/reviews/v2.5.11-remediation-report.md
- docs/reviews/v2.5.11-security-validation.md
- docs/reviews/v2.5.11-release-readiness.md
- docs/architecture/versioning.md
- docs/architecture/report-artifact-authorization.md
- docs/architecture/data-provenance.md
- docs/operations/safe-sync.md
- docs/operations/release-process.md
- docs/operations/encoding-standard.md
- artifacts/test-results/v2.5.11-test-results.json
- artifacts/test-results/v2.5.11-release-gates.json

Follow existing repository conventions if artifact directories differ.

Do not commit temporary reports, raw customer data, credentials, tokens, local
tenant overrides, or local authentication files.

==================================================
RELEASE GATES
==================================================

GATE 1: VERSION CONSISTENCY

Pass only if version, release, channel, build and commit are consistent across
manifest, API, UI, documentation, report and QA.

GATE 2: ENCODING

Pass only if Turkish characters render correctly across JSON, API, PowerShell,
HTML and PDF.

GATE 3: GIT SAFETY

Pass only if automatic sync cannot push to main or create tags.

GATE 4: REPORT AUTHORIZATION

Pass only if report download uses report ID and checks tenant authorization.

GATE 5: UI DATA INTEGRITY

Pass only if no static live metric or personal default identity is present.

GATE 6: QA INTEGRITY

Pass only if structured results, real durations, commit binding and manifest
binding are implemented.

GATE 7: DATA PROVENANCE

Pass only if customer-facing KPI values have source and collection metadata.

GATE 8: COMPLIANCE WORDING

Pass only if absolute regulatory compliance and inaccurate nonrepudiation claims
are removed.

GATE 9: REGRESSION

Pass only if existing tenant isolation, concurrency, report generation, PDF and
HTML functions continue to pass.

GATE 10: PILOT READINESS

Pass only if:

- zero Critical findings remain
- zero unaccepted High findings remain
- all required tests pass
- productionReady remains false
- release is v2.5.11-PILOT
- pilot limitations are documented

==================================================
FINAL RESPONSE FORMAT
==================================================

After completing the implementation, respond with:

1. Baseline commit and version
2. Architecture changes
3. Files modified
4. Files created
5. Version consistency correction
6. Git synchronization correction
7. Encoding root cause
8. Encoding correction
9. Report download authorization design
10. Removed static UI data
11. Removed personal defaults
12. QA protocol changes
13. Data provenance changes
14. Compliance wording changes
15. Tests added
16. Commands executed
17. Test results
18. Failed or blocked checks
19. Remaining risks
20. Release gate table
21. Target release
22. Final recommendation

Final recommendation must be exactly one of:

- ReadyForControlledPilot
- NotReady
- BlockedByExternalDependency

Do not use ReadyForControlledPilot if encoding remains broken.

Do not use ReadyForControlledPilot if auto-sync can still push directly to main.

Do not use ReadyForControlledPilot if path-based public report download remains.

Do not use ReadyForControlledPilot if static live metrics remain in production
HTML.

Do not increment to PRODUCTION.

Keep:

productionReady = false
channel = pilot
environment = pilot

Do not claim deployment was completed unless deployment was actually executed
and the deployed /api/version endpoint was verified.